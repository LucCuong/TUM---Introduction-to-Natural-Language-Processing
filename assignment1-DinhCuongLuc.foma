! Read the lexicon definition file
read lexc assignment1-DinhCuongLuc.lexc
define Lexicon;

!define EInsertion [..] -> e || a t _ "^" s;

define ERemove e -> 0 || t _ "^" ( o r | i o n ) ,,
               e -> 0 || t _ a b ;

define Cleanup "^" -> 0;

regex Lexicon           .o.
      EInsertion        .o.
      ERemove           .o.
      Cleanup;



















